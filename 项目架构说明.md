# UMich Study Spaces 项目架构说明 / Project Architecture Documentation

## 📋 项目概述 / Project Overview

这是一个**全栈学习空间探索应用**，帮助密歇根大学学生找到和分享校园内的学习空间。项目采用现代化的技术栈，包含前端、后端、数据库和认证系统。

This is a **full-stack study space exploration application** that helps University of Michigan students find and share study spaces across campus. The project uses modern technology stack including frontend, backend, database, and authentication system.

---

## 🏗️ 技术栈 / Technology Stack

### 前端 / Frontend
- **React 18** + **TypeScript** - 现代 React 框架和类型安全 / Modern React framework with type safety
- **Vite** - 快速的前端构建工具（端口 8080）/ Fast frontend build tool (Port 8080)
- **React Router** - 单页应用路由 / Single-page application routing
- **TanStack Query (React Query)** - 数据获取和状态管理 / Data fetching and state management
- **shadcn/ui** - 基于 Radix UI 的组件库 / Component library based on Radix UI
- **Tailwind CSS** - 实用优先的 CSS 框架 / Utility-first CSS framework
- **Clerk** - 用户认证和授权 / User authentication and authorization

### 后端 / Backend
- **Node.js** + **Express** - RESTful API 服务器（端口 3001）/ RESTful API server (Port 3001)
- **PostgreSQL (Neon)** - 云端数据库 / Cloud database
- **Multer** - 文件上传处理 / File upload handling
- **Clerk SDK** - 后端认证中间件 / Backend authentication middleware

---

## 📁 项目目录结构 / Project Directory Structure

```
UMich Study Spaces Project/
├── server/                    # 后端服务器 / Backend Server
│   ├── server.js             # Express 服务器主文件 / Express server main file
│   ├── package.json          # 后端依赖 / Backend dependencies
│   └── uploads/              # 上传的文件存储 / Uploaded files storage
│       └── reviews/          # 用户评论照片 / User review photos
│
├── src/                      # 前端源代码 / Frontend Source Code
│   ├── main.tsx             # 应用入口（Clerk 配置）/ App entry (Clerk config)
│   ├── App.tsx              # 路由配置 / Route configuration
│   │
│   ├── pages/               # 页面组件 / Page Components
│   │   ├── Home.tsx         # 首页（校园选择）/ Home page (campus selection)
│   │   ├── Campus.tsx       # 校区页面（建筑列表）/ Campus page (building list)
│   │   ├── Building.tsx     # 建筑详情页 / Building detail page
│   │   ├── AreaDetail.tsx   # 学习空间详情页 / Study space detail page
│   │   ├── Favorites.tsx    # 收藏列表 / Favorites list
│   │   ├── Submit.tsx       # 提交新空间 / Submit new space
│   │   ├── About.tsx        # 关于页面 / About page
│   │   ├── Auth.tsx         # 认证页面 / Authentication page
│   │   ├── AdminSubmissions.tsx  # 管理员提交审核 / Admin submission review
│   │   └── NotFound.tsx     # 404 页面 / 404 page
│   │
│   ├── components/          # 可复用组件 / Reusable Components
│   │   ├── Header.tsx       # 导航栏 / Navigation bar
│   │   ├── Footer.tsx       # 页脚 / Footer
│   │   ├── SpaceCard.tsx    # 学习空间卡片 / Study space card
│   │   ├── FilterPanel.tsx  # 筛选面板 / Filter panel
│   │   ├── PhotoCarousel.tsx # 照片轮播 / Photo carousel
│   │   └── ui/              # shadcn/ui 基础组件 / shadcn/ui base components
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       ├── dialog.tsx
│   │       └── ... (其他 UI 组件 / other UI components)
│   │
│   ├── data/                # 静态数据 / Static Data
│   │   ├── buildings.ts     # 建筑信息 / Building information
│   │   ├── studyAreas.ts    # 学习空间数据 / Study space data
│   │   └── filterOptions.ts # 筛选选项定义 / Filter options definition
│   │
│   ├── lib/                 # 工具库 / Utility Library
│   │   ├── api.ts           # API 客户端（封装后端调用）/ API client (wraps backend calls)
│   │   ├── auth.ts          # 认证工具 / Authentication utilities
│   │   ├── utils.ts         # 通用工具函数 / General utility functions
│   │   └── clerk-init.ts    # Clerk 初始化 / Clerk initialization
│   │
│   ├── types/               # TypeScript 类型定义 / TypeScript Type Definitions
│   │   └── StudySpace.ts    # 学习空间类型 / Study space types
│   │
│   ├── hooks/               # React Hooks
│   │   ├── use-mobile.tsx   # 响应式检测 / Responsive detection
│   │   └── use-toast.ts     # 通知提示 / Toast notifications
│   │
│   └── utils/               # 工具函数 / Utility Functions
│       └── imageUtils.ts    # 图片处理工具 / Image processing utilities
│
├── public/                  # 静态资源 / Static Assets
│   ├── building photos/     # 建筑照片 / Building photos
│   ├── Campus photos/       # 校区照片 / Campus photos
│   └── photos/              # 学习空间照片（按校区/建筑分类）/ Study space photos (by campus/building)
│
├── package.json             # 前端依赖和脚本 / Frontend dependencies and scripts
├── vite.config.ts          # Vite 配置（端口 8080）/ Vite config (port 8080)
└── start.sh                # 启动脚本（同时启动前后端）/ Startup script (starts both frontend & backend)
```

---

## 🔄 应用架构流程 / Application Architecture Flow

### 1. 用户访问流程 / User Access Flow
```
浏览器 / Browser → Vite 开发服务器 (8080) / Vite Dev Server (8080)
                → React Router 路由匹配 / React Router route matching
                → 页面组件渲染 / Page component rendering
                → 调用 API (3001) / Call API (3001)
                → 后端查询数据库 / Backend queries database
                → 返回数据 / Return data
                → 前端展示 / Frontend display
```

### 2. 认证流程 / Authentication Flow
```
用户登录 / User login → Clerk 认证服务 / Clerk auth service
                      → 返回 JWT Token / Return JWT Token
                      → 前端存储 Token / Frontend stores Token
                      → API 请求携带 Token / API requests with Token
                      → 后端验证 Token (Clerk SDK) / Backend verifies Token (Clerk SDK)
                      → 授权访问 / Authorized access
```

### 3. 数据流程 / Data Flow
```
静态数据 (buildings.ts, studyAreas.ts) / Static Data
    ↓
动态数据 (PostgreSQL 数据库) / Dynamic Data (PostgreSQL Database)
    ↓
后端 API (server.js) / Backend API (server.js)
    ↓
前端 API 客户端 (lib/api.ts) / Frontend API Client (lib/api.ts)
    ↓
React Query 缓存 / React Query Cache
    ↓
UI 组件展示 / UI Component Display
```

---

## 🎯 核心功能模块 / Core Feature Modules

### 1. 学习空间浏览 / Study Space Browsing (`pages/`)
- **Home.tsx**: 首页，展示两个校区入口 / Home page, displays two campus entrances
- **Campus.tsx**: 展示校区内所有建筑 / Displays all buildings in campus
- **Building.tsx**: 展示建筑内的学习空间列表 / Displays study space list in building
- **AreaDetail.tsx**: 学习空间详细信息（照片、评论、评分）/ Study space detail (photos, reviews, ratings)

### 2. 筛选功能 / Filtering Feature (`components/FilterPanel.tsx`)
- 噪音水平 (Noise Level)
- 隐私程度 (Privacy Level)
- 适合用途 (Best For)
- 便利设施 (Amenities)
- 可预订房间 (Bookable Rooms)
- 封闭程度 (Enclosed Level)

### 3. 用户功能 / User Features
- **认证 / Authentication**: Clerk 集成 (`lib/auth.ts`, `main.tsx`) / Clerk integration
- **收藏 / Favorites**: 收藏/取消收藏学习空间 (`pages/Favorites.tsx`) / Add/remove favorites
- **评论 / Reviews**: 添加评论、上传照片、点赞 (`AreaDetail.tsx`) / Add reviews, upload photos, like
- **提交 / Submit**: 用户提交新的学习空间 (`pages/Submit.tsx`) / User submits new study space

### 4. 管理员功能 / Admin Features
- **审核提交 / Review Submissions**: 审核用户提交的新空间 (`pages/AdminSubmissions.tsx`) / Review user-submitted spaces

---

## 🔌 API 接口 / API Endpoints (后端 `server/server.js` / Backend)

### Study Spaces API
- `GET /api/study-spaces` - 获取所有学习空间 / Get all study spaces
- `GET /api/study-spaces/:id` - 获取单个学习空间 / Get single study space

### Reviews API
- `GET /api/areas/:areaId/reviews` - 获取空间评论 / Get space reviews
- `POST /api/reviews/upload-photos` - 上传评论照片（需认证）/ Upload review photos (auth required)
- `POST /api/areas/:areaId/reviews` - 创建评论（需认证）/ Create review (auth required)
- `PUT /api/reviews/:reviewId/helpful` - 点赞评论（需认证）/ Mark review helpful (auth required)
- `DELETE /api/reviews/:reviewId` - 删除评论（需认证）/ Delete review (auth required)

### Favorites API
- `GET /api/users/:userId/favorites` - 获取用户收藏（需认证）/ Get user favorites (auth required)
- `POST /api/users/:userId/favorites` - 添加收藏（需认证）/ Add favorite (auth required)
- `DELETE /api/users/:userId/favorites/:spaceId` - 删除收藏（需认证）/ Remove favorite (auth required)

### Submissions API
- `GET /api/submissions` - 获取所有提交（管理员）/ Get all submissions (admin)
- `GET /api/submissions/:id` - 获取单个提交（管理员）/ Get single submission (admin)
- `POST /api/submissions` - 创建提交（需认证）/ Create submission (auth required)
- `PUT /api/submissions/:id/status` - 更新提交状态（管理员）/ Update submission status (admin)

### Health Check
- `GET /api/health` - 健康检查 / Health check

---

## 💾 数据库结构 / Database Structure (PostgreSQL)

### 主要表 / Main Tables
- **study_spaces**: 学习空间信息 / Study space information
- **reviews**: 用户评论 / User reviews
- **favorites**: 用户收藏 / User favorites
- **submissions**: 用户提交的新空间 / User-submitted new spaces
- **profiles**: 用户资料（与 Clerk 集成）/ User profiles (integrated with Clerk)

---

## 🔐 认证与授权 / Authentication & Authorization

### Clerk 集成 / Clerk Integration
- **前端 / Frontend**: `main.tsx` 中使用 `ClerkProvider` 包裹应用 / Wrap app with `ClerkProvider` in `main.tsx`
- **后端 / Backend**: `server.js` 中使用 `ClerkExpressRequireAuth()` 中间件 / Use `ClerkExpressRequireAuth()` middleware in `server.js`
- **用户信息 / User Info**: 通过 `req.auth` 获取用户信息 / Get user info via `req.auth`

### 认证流程 / Authentication Flow
1. 用户在前端点击登录 → Clerk 弹出登录窗口 / User clicks login → Clerk shows login window
2. Clerk 验证后返回 Token / Clerk returns Token after verification
3. 前端在 API 请求中携带 Token（`lib/api.ts`）/ Frontend includes Token in API requests (`lib/api.ts`)
4. 后端验证 Token 并获取用户信息 / Backend verifies Token and gets user info

---

## 🎨 UI/UX 设计 / UI/UX Design

### 设计系统 / Design System
- **主色调 / Primary Color**: 密歇根大学蓝色 (`#00274C`) / University of Michigan Blue
- **强调色 / Accent Color**: 金色 (`#FFCB05`) / Gold
- **组件库 / Component Library**: shadcn/ui（基于 Radix UI）/ shadcn/ui (based on Radix UI)
- **样式 / Styling**: Tailwind CSS

### 响应式设计 / Responsive Design
- 移动端优先 / Mobile-first
- 使用 `use-mobile` Hook 检测设备 / Use `use-mobile` Hook to detect device
- Header 组件包含移动端菜单 (`Sheet`) / Header component includes mobile menu (`Sheet`)

---

## 📦 数据管理 / Data Management

### 静态数据 / Static Data
- **buildings.ts**: 建筑信息（名称、地址、描述等）/ Building info (name, address, description)
- **studyAreas.ts**: 学习空间详细信息 / Study space detailed information
- **filterOptions.ts**: 筛选选项枚举 / Filter options enumeration

### 动态数据 / Dynamic Data
- 通过 PostgreSQL 数据库存储 / Stored in PostgreSQL database
- 使用 React Query 进行缓存和状态管理 / Use React Query for caching and state management
- API 调用封装在 `lib/api.ts` / API calls wrapped in `lib/api.ts`

---

## 🚀 启动流程 / Startup Process

1. **启动脚本 / Startup Script** (`start.sh`):
   - 检查端口占用 / Check port occupancy
   - 启动后端服务器 (3001) / Start backend server (3001)
   - 启动前端开发服务器 (8080) / Start frontend dev server (8080)

2. **后端启动 / Backend Startup**:
   - 加载环境变量 (`.env`) / Load environment variables (`.env`)
   - 连接 PostgreSQL 数据库 / Connect to PostgreSQL database
   - 初始化 Express 服务器 / Initialize Express server
   - 配置 CORS、文件上传等中间件 / Configure CORS, file upload middleware

3. **前端启动 / Frontend Startup**:
   - Vite 编译 React 应用 / Vite compiles React app
   - 加载 Clerk 配置 / Load Clerk configuration
   - 初始化路由和全局状态 / Initialize routing and global state

---

## 🔧 配置文件 / Configuration Files

- **vite.config.ts**: Vite 配置（端口 8080，路径别名）/ Vite config (port 8080, path aliases)
- **tailwind.config.ts**: Tailwind CSS 配置 / Tailwind CSS configuration
- **tsconfig.json**: TypeScript 配置 / TypeScript configuration
- **package.json**: 前端依赖和脚本 / Frontend dependencies and scripts
- **server/package.json**: 后端依赖 / Backend dependencies

---

## 📝 关键文件说明 / Key Files Explanation

### 前端核心文件 / Frontend Core Files
- `src/main.tsx`: 应用入口，初始化 Clerk 和 React / App entry, initializes Clerk and React
- `src/App.tsx`: 路由配置，定义所有页面路由 / Route configuration, defines all page routes
- `src/lib/api.ts`: API 客户端，封装所有后端调用 / API client, wraps all backend calls
- `src/types/StudySpace.ts`: TypeScript 类型定义 / TypeScript type definitions

### 后端核心文件 / Backend Core Files
- `server/server.js`: Express 服务器，包含所有 API 路由 / Express server, contains all API routes
- 使用 PostgreSQL 连接池管理数据库连接 / Use PostgreSQL connection pool for database connections
- Multer 处理文件上传（评论照片）/ Multer handles file uploads (review photos)

---

## 🎓 项目特点 / Project Features

1. **全栈架构 / Full-Stack Architecture**: 前后端分离，清晰的 API 设计 / Frontend-backend separation, clear API design
2. **类型安全 / Type Safety**: TypeScript 贯穿整个项目 / TypeScript throughout the project
3. **现代化 UI / Modern UI**: 使用 shadcn/ui 组件库 / Use shadcn/ui component library
4. **用户认证 / User Authentication**: 集成 Clerk，无需自建认证系统 / Integrate Clerk, no need to build auth system
5. **数据管理 / Data Management**: React Query 优化数据获取和缓存 / React Query optimizes data fetching and caching
6. **文件上传 / File Upload**: 支持用户上传照片并存储 / Support user photo uploads and storage
7. **响应式设计 / Responsive Design**: 适配桌面和移动端 / Adapts to desktop and mobile

---

## 🔍 代码组织原则 / Code Organization Principles

1. **组件化 / Componentization**: 可复用组件放在 `components/` / Reusable components in `components/`
2. **页面分离 / Page Separation**: 每个路由对应一个页面组件 / Each route corresponds to a page component
3. **API 封装 / API Wrapping**: 所有 API 调用统一在 `lib/api.ts` / All API calls unified in `lib/api.ts`
4. **类型定义 / Type Definitions**: 集中管理 TypeScript 类型 / Centralized TypeScript type management
5. **静态数据 / Static Data**: 建筑和学习空间数据与代码分离 / Building and study space data separated from code

---

## 📚 学习要点 / Learning Points

- **React Router**: 学习如何配置单页应用路由 / Learn how to configure SPA routing
- **React Query**: 学习如何进行数据获取和缓存 / Learn data fetching and caching
- **Clerk**: 学习如何集成第三方认证服务 / Learn how to integrate third-party auth service
- **Express**: 学习如何构建 RESTful API / Learn how to build RESTful APIs
- **PostgreSQL**: 学习如何连接和查询数据库 / Learn database connection and queries
- **文件上传 / File Upload**: 学习如何使用 Multer 处理文件上传 / Learn how to use Multer for file uploads
- **TypeScript**: 学习如何为 React 应用添加类型安全 / Learn how to add type safety to React apps
